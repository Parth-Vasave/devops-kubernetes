apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Log output with a serverless Ping-pong on Knative Serving (Exercise 5.7).
# The database secret is applied separately: sops --decrypt ../ping-pong/manifests/secret.enc.yaml | kubectl apply -f -
resources:
  - namespace.yaml
  - postgres.yaml
  - pingpong.yaml
  - log-output.yaml
